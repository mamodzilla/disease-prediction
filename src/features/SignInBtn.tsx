import React from "react";
import "../app/styles/sign-in-btn.css";

const SignInBtn: React.FC = () => {
    return (
        <button className="sign-in__btn" type="submit">Login</button>
    );
};

export default SignInBtn;