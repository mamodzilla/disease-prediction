import React from "react";
import "../app/styles/progress-bar.css";

const ProgressBar: React.FC = () => {
    return (
        <div className="by-symptoms__progress-bar-container">
            At this stage, general questions are asked and the location of the problem is determined
        </div>
    );
};

export default ProgressBar;